<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Eri.Views.ItemDetailPage"
             Title="">
    

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="40">
            <Label Text="金額" FontSize="Medium"/>
            <Label Text="{Binding Money, StringFormat='{0:#,0} 円'}" FontSize="17" FontAttributes="Bold" />
            <Label Text="支給日" FontSize="Medium" />
            <Label Text="{Binding Payment_Date, StringFormat='{0:yyyy}年{0:MM}{0:dd}日'}"  FontSize="17" FontAttributes="Bold" />
            <Label Text="説明" FontSize="Medium" />
            <Label Text="{Binding Description}"  FontSize="17" FontAttributes="Bold"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>